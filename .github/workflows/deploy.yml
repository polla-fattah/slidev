name: Deploy Slidev

on:
  push:
    branches:
      - main  # or another branch you use

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Setup Node
        uses: actions/setup-node@v3
        with:
          node-version: 18

      - name: Install Slidev
        run: npm install -g @slidev/cli

      - name: Build Slidev
        # Replace 'slidev' below with your actual repo name if it's different
        run: slidev build --base /slidev/

      - name: Deploy to GitHub Pages
        uses: peaceiris/actions-gh-pages@v3
        with:
          personal_token: ${{ secrets.GH_PAT }}  # Must have 'repo' scope
          publish_dir: ./dist
          publish_branch: gh-pages
          keep_files: false
